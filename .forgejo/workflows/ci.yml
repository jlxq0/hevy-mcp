name: CI

on:
  push:
    branches: [main]
    tags: ["v*"]
  pull_request:

permissions:
  contents: read

env:
  CARGO_TERM_COLOR: always
  RUSTFLAGS: "-Dwarnings"

jobs:
  # All cargo checks in one job — cache hit + shared incremental state beats
  # one-job-per-check. hevy-mcp has no C deps, so no cmake/apt step.
  cargo:
    runs-on: ubuntu-22.04
    steps:
      - uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5  # v4
        with: { persist-credentials: false }
      # Pin a commit on master, not on `stable`. Forge resolves actions from its
      # own mirrors (DEFAULT_ACTIONS_URL = forge.oddie.app), and dtolnay
      # force-pushes `stable`, so the mirror's 24h sync prunes the old tip and
      # the pin 404s. master/v1 is append-only. It has no default `toolchain`.
      #
      # An exact version, not `stable`. With `RUSTFLAGS: -Dwarnings` a moving
      # stable means the next clippy release fires a new lint on untouched code
      # and reds whatever pull request happens to be open, and each `#[allow]`
      # added to quiet one raises the version below which nobody can lint the
      # tree at all — silently, because CI is always on the new side of it.
      # Bump this deliberately, in its own change, and read AGENTS.md first.
      - uses: dtolnay/rust-toolchain@6c977a6ca4077a0ceb28ffbe03f59d46e9ac8772  # v1
        with:
          toolchain: "1.98.0"
          components: rustfmt, clippy
      - uses: actions/cache@0057852bfaa89a56745cba8c7296529d2fc39830  # v4
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
            ~/.cargo/bin
            target
          key: cargo-${{ runner.os }}-${{ hashFiles('**/Cargo.lock') }}
          restore-keys: cargo-${{ runner.os }}-
      - name: fmt
        run: cargo fmt --all --check
      - name: clippy
        run: cargo clippy --all-targets --all-features --locked -- -D warnings
      - name: test
        run: cargo test --all-features --locked
      # Two steps on purpose. A single `cargo audit` is red both when a
      # vulnerability exists and when the advisory database cannot be fetched,
      # and those want opposite responses. Job logs are not retrievable on this
      # instance, so the step name is the only diagnostic anyone gets.
      # `99a213b` is the specimen: `cargo (main)` failed while `cargo (v0.4.2)`
      # passed on the same commit eighteen seconds later, and reproducing it
      # locally gave `couldn't fetch advisory database: git operation failed`,
      # then two clean runs.
      - name: fetch advisory database
        run: |
          set -euo pipefail
          DB="$(mktemp -d)/advisory-db"
          git clone --depth 1 https://github.com/RustSec/advisory-db "$DB"
          echo "ADVISORY_DB=$DB" >> "$GITHUB_ENV"
      - name: audit
        run: |
          set -euo pipefail
          [ -x ~/.cargo/bin/cargo-audit ] || cargo install --locked cargo-audit
          cargo audit --no-fetch --db "$ADVISORY_DB"
      - uses: EmbarkStudios/cargo-deny-action@6c8f9facfa5047ec02d8485b6bf52b587b7777d1  # v2
        with: { command: check bans licenses sources }

  # buildctl talks to Pada's buildkitd over TCP. Registry-backed layer cache.
  docker:
    runs-on: ubuntu-22.04
    needs: cargo
    permissions: { contents: read, packages: write }
    steps:
      - uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5  # v4
        with: { persist-credentials: false }
      - name: Install buildctl
        env:
          BUILDKIT_VERSION: v0.30.0
          BUILDKIT_SHA256: 2da148c50540409988c837e62b72176e4a9ad7855548a2dd6ea1cd0c0d2d360d
        run: |
          set -euo pipefail
          TARBALL="/tmp/buildkit.tar.gz"
          curl -sSL -o "$TARBALL" \
            "https://github.com/moby/buildkit/releases/download/${BUILDKIT_VERSION}/buildkit-${BUILDKIT_VERSION}.linux-amd64.tar.gz"
          echo "${BUILDKIT_SHA256}  ${TARBALL}" | sha256sum -c -
          sudo tar -xz -C /usr/local --strip-components=0 -f "$TARBALL"
          rm -f "$TARBALL"
          buildctl --version
      - name: Login to Forgejo registry
        if: github.event_name != 'pull_request'
        run: |
          mkdir -p ~/.docker
          F_AUTH=$(printf "%s:%s" "jlxq0" "${{ secrets.FORGE_PUSH_TOKEN }}" | base64 -w0)
          echo "{\"auths\":{\"forge.oddie.app\":{\"auth\":\"$F_AUTH\"}}}" > ~/.docker/config.json
      - name: Build (and push on tag)
        run: |
          set -euo pipefail
          # buildkitd runs on the runner host, listening on 0.0.0.0:1234. From
          # inside this job container the host is our own bridge gateway — never
          # a hardcoded IP (those change on reboot/DHCP and differ per repo).
          # The image is slim (no iproute2), so read the default route from
          # /proc/net/route via node (always present — actions run on it).
          GW="$(node -e 'const fs=require("fs");for(const l of fs.readFileSync("/proc/net/route","utf8").split("\n").slice(1)){const f=l.trim().split(/\s+/);if(f[1]==="00000000"){const h=f[2];console.log([h.slice(6,8),h.slice(4,6),h.slice(2,4),h.slice(0,2)].map(x=>parseInt(x,16)).join("."));break;}}')"
          export BUILDKIT_HOST="tcp://${GW}:1234"
          echo "buildkitd at ${BUILDKIT_HOST}"
          CACHE_REF="forge.oddie.app/jlxq0/hevy-mcp:buildcache"
          IMPORT_CACHE_ARG=()
          EXPORT_CACHE_ARG=()
          if [[ "$GITHUB_REF" == refs/tags/v* ]]; then
            V="${GITHUB_REF#refs/tags/}"
            TAGS="forge.oddie.app/jlxq0/hevy-mcp:${V},forge.oddie.app/jlxq0/hevy-mcp:latest"
            PUSH=true
            # Import only. A tag build is the same commit as the `main` build
            # that preceded it — the branch differs only in TAGS and PUSH, which
            # are output settings and not build inputs — so its cache would be
            # byte-identical to one already written. Exporting it was pure
            # duplication, and the duplication was the race. See the comment on
            # the export below.
            IMPORT_CACHE_ARG=(--import-cache "type=registry,ref=${CACHE_REF}")
          elif [[ "$GITHUB_REF" == refs/heads/main ]]; then
            TAGS="forge.oddie.app/jlxq0/hevy-mcp:ci-${GITHUB_SHA::8}"
            PUSH=false
            IMPORT_CACHE_ARG=(--import-cache "type=registry,ref=${CACHE_REF}")
            # The only writer. Merging a pull request and pushing the release
            # tag are two pushes, so two `docker` jobs run at once; both
            # exporting to one unqualified `:buildcache` ref made one lose the
            # blob write. One writer removes the race by construction rather
            # than by scheduling.
            EXPORT_CACHE_ARG=(--export-cache "type=registry,ref=${CACHE_REF},mode=max")
          else
            TAGS="forge.oddie.app/jlxq0/hevy-mcp:ci-${GITHUB_SHA::8}"
            PUSH=false
          fi
          buildctl build \
            --frontend dockerfile.v0 \
            --local context=. \
            --local dockerfile=. \
            "${IMPORT_CACHE_ARG[@]}" \
            "${EXPORT_CACHE_ARG[@]}" \
            --output "type=image,\"name=${TAGS}\",push=${PUSH}"
